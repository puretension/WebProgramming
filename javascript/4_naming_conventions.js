/**
 * Naming Conventions
 * 
 * 1) 영어로 하자(당연).
 * 2)특수기호는 $, _ 사용 가능.
 * 3) 숫자로 이름을 시작 할 수 없다.
 * 4) 키워드는 변수명으로 사용 불가. var const = 'var';
 */

let pureTension = 'pureTension';
var $ive = '아이브';
const _yujin = '안유진';

console.log(pureTension);
console.log($ive);
console.log(_yujin);